{% extends 'base.html.twig' %}

{% block title %} Hello ArticleController! {% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

 <div class="example-wrapper">
    <h1> {{ article.name }} </h1> <br />
    <span> Publié par {{user.firstname}} {{user.lastname}}</span> <br />
    <span> Date de publication {{ (article.createdAt is empty) ? 'NC' : article.createdAt|date }} </span> 



   {# <ul>
        <li> {{user.firstname}} </li>
        <li> {{user.lastname}} </li>
        <li> {{user.email}} </li>
        <li> {{user.birthday | date}} </li>
        <ul> <b><u> Liste d'article </u></b>

        {% for article in user.articles %}
            <li>
                <a href="{{ path("articleBy")}}" > {{article.id}}  - {{article.name}}  -  {{article.createdAt|date}} </a>
            </li>
        {% endfor %} 
        </ul>
    </ul>
<hr />
    {# {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }} 

</div> #}
{% endblock %}