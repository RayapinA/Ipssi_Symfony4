{% extends 'base.html.twig' %}


{% block body %}
 <ul>
        <li> {{app.user.firstname}} </li>
        <li> {{app.user.lastname}} </li>
        <li> {{app.user.email}} </li>
        <li> {{app.user.birthday | date}} </li>
        <ul> <b><u> Liste d'article </u></b>

        {% for article in app.user.articles %}
            <li>
                <a href="{{ path("articleBy")}}" > {{article.id}}  - {{article.name}}  -  {{article.createdAt|date}} </a>
            </li>
        {% endfor %} 
        </ul>
    </ul>
<hr />

{% endblock %}